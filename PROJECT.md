# Yaniv Scorekeeper

A fast, offline scorekeeping app for live games of Yaniv — the Israeli card game. Replaces the paper scoresheet; does not simulate the game.

## Business Purpose

Yaniv is played with physical cards, but the scoring is fiddly to track by hand: detecting an **Assaf** (a failed Yaniv call), applying the **+30 penalty**, handling the **exact-100 score-halving** house rule, and keeping cumulative totals straight across an open-ended number of rounds. This app does that math automatically so players can focus on the game. It does **not** deal, hold, or simulate cards — the cards stay on the table.

## Target Users

Casual Yaniv players — the game is popular among Israeli backpackers, soldiers, and students. One person ("the scorekeeper") holds a phone at the table and records each round. Typical conditions: a single device, often **no internet** (hostels, bases, buses). Speed and offline reliability matter more than features.

## Features

**v1 scope (agreed 2026-06-02):**

- **Game setup** — add 2 to 6+ players by name (no hard cap at 5); choose Yaniv threshold (7 default, or 5 / 11); toggle the 100-halving house rule; optionally set an elimination/knockout score.
- **Round entry** — tap who called "Yaniv!", then enter each player's revealed hand total. The app resolves the round (see Scoring Engine) and updates cumulative scores.
- **Mid-game join** — a player can join a game already in progress. They start seeded at the **highest cumulative score among the still-active (non-eliminated) players** at the moment they join, so a latecomer gets no unfair head start. They take the next seat — the seating circle grows. (See Scoring Engine for the precise seeding rules.)
- **Automatic scoring** — successful Yaniv vs. Assaf detection, +30 penalty, hand-value scoring for everyone else, and the exact-100 halving rule (any positive multiple of 100 — 100, 200, 300, 400, … with no upper limit) applied with a visible callout.
- **Live standings** — running cumulative scores shown in **static seating order matching the physical table** (the order players sit around it), **never reordered by score**. The leader is only *indicated* — a crown/highlight — never repositioned to the top. A clear indicator shows who starts the next round.
- **Round history — the vertical scoresheet** — the Big Board view is a full scoresheet grid, the digital twin of the handwritten paper sheet: players as columns (static seating order), rounds as rows down the page (oldest at top), each cell the running cumulative total after that round, a left round column noting who called and whether it was a Yaniv or an Assaf, and in-cell markers for the special moments (Assaf +30, 100-halving, elimination, mid-game join). The scoresheet IS the round-by-round history.
- **Undo / edit last round** — mis-entered totals are common; the scorekeeper can correct the most recent round and have everything recompute.
- **Elimination & game end** — open-ended play; optional knockout threshold removes a player who crosses it; "end game" crowns the lowest cumulative score as winner at any time.
- **End-of-game stats** — the final screen shows each player's count of successful "Yaniv!" calls during the game (a per-game stat, computed from this game's round history — no cross-game database needed).
- **Online landing page** — the public URL the app is shared by. Before installing, a visitor sees what the app is, why to install it (works offline, full-screen, no app store), **step-by-step install instructions for both Android and iPhone/iPad**, and **both** guides: **How to Use** (operating the app) and **How to Play** (the rules of Yaniv). This page is how every device gets its own copy (see Distribution model).
- **In-app Help screen** — reachable any time from inside the installed app via a **"?" / Help** control on the Play and Setup screens. It carries **two clearly-separated, independently-titled bodies — "How to Use" (how to operate the scorekeeper) and "How to Play" (the rules of the game, drawn from the Yaniv rules)** — on two labelled tabs/sections, plus a "share this app" link. It omits the install steps (you're already installed). Both bodies are authored **once** and shared between the landing page and the in-app screen so they can never drift apart. (Drafted copy: `docs/landing-and-help-content.md`, Section A = How to Use, Section B = How to Play.)

**Deferred (not in v1):**

- Player stats / cross-game history (wins, averages, Assaf counts) — deliberately out, so the app needs no cross-game database.
- Multi-device sync, accounts, online play.
- Any card-game simulation (dealing, hand validation, run/set legality).

## Scoring Engine (the core logic)

For a round where the caller's hand value is `C` and the lowest *other* player's hand value is `L`:

1. **Successful Yaniv** — `C < L` (caller is strictly lower than *every* other player):
   - Caller scores **0**.
   - Every other player scores their own hand value.
   - Caller starts the next round.
2. **Assaf** — `C >= L` (any other player ties or beats the caller):
   - Caller scores **`C + 30`**.
   - Every other player (including the catcher) scores their own hand value.
   - The catcher starts the next round.
3. **Cumulative update + 100-halving** — add each player's round score to their cumulative total, then for any player whose new total is **exactly** a positive multiple of 100 (100, 200, 300, 400, … — no upper limit), halve it and show a callout (e.g. "Dana hit 100 → halved to 50").
4. **Elimination check** (if a knockout score is set) — any player whose cumulative total crosses the threshold is marked out.

**Edge-case rule (default, overridable as a house rule later):** if an Assaf has multiple catchers (several players tie or beat the caller), the **lowest hand among the catchers** starts the next round; ties broken by seating order after the caller.

**Mid-game join — seeding rules:**

1. **Seed score** — a joiner starts at the **highest cumulative score among the still-active (non-eliminated) players** at the moment of joining. This prevents a latecomer gaining an unfair head start over the field.
2. **No halving on the seed** — the seeded starting score does **not** trigger the 100-halving rule, even if the seed is itself an exact multiple of 100. Halving applies only to scores produced by playing rounds, not to the entry seed.
3. **Normal play thereafter** — from the join onward the player is treated like any other. Future round scores that land their cumulative total on an exact multiple of 100 **do** halve as usual.
4. **Never instantly eliminated** — a joiner is never knocked out at the moment of joining, even if their seed equals or sits at/below an elimination threshold that a struggling player is already near. They enter the game in play.
5. **Replayable & derived** — the join is recorded as an event in the round history, so undo/edit and recompute-from-history continue to hold. The seed is **derived at recompute time** from the active players' totals as of the join event — it is **never stored** — so editing earlier rounds keeps the seed correct.

## UX Considerations

- **Mobile-first** — designed for one phone held at a table; large tap targets, minimal typing, fast round entry.
- **Offline-first** — fully usable with no connection; installable to the home screen (PWA).
- **Crash-safe** — game state persists to local storage on every change, so an accidental refresh or app-close never loses the game.
- **Legible at a glance** — standings and "who's next" readable across the table; clear, celebratory callouts for Assaf and 100-halving moments.
- Accessibility: high contrast, readable font sizes, works one-handed.

### UI Design Direction (approved 2026-06-02)

Yaniv **deliberately diverges** from Roger's executive house style. It is a casual party/card game, so the UI takes on a fun, gamey personality (playful colour, chunky rounded controls, card-table motifs, celebratory moments) rather than the corporate dashboard look. The **WCAG 2.1 AA accessibility floor still applies in full** — fun must never cost across-table legibility, which is the whole point of the app.

- **Two user-toggleable themes on one shared, tokenized theme layer** (same layout and components; only the skin swaps): **A — "Felt & Chips"** (card-table felt, brass, ivory score chips) and **B — "Party Arcade"** (bright, loud, chunky, confetti). The toggle is a **persisted per-device preference**. Both themes are held to AA contrast *and* across-table legibility — B is held to the same legibility bar despite being louder.
- **Two views, switched by activity:**
  - **Circle view — for VIEWING during the game.** The phone lies flat on the table; players are arranged around a ring with each player's score oriented to face their own seat; the scorekeeper's seat sits upright at the bottom. Readable from all sides.
  - **Table / list view — for ENTERING scores.** Scorekeeper-facing and upright, with the custom on-screen number pad and the confirm-round step.
- **Static seating order in both views.** Neither view ever reorders players by score; seating is the stable anchor at the table, and the leader is only *indicated* (crown/highlight), never repositioned.

The full design proposal — themes, layouts, and rationale — is in `docs/ui-direction.md`.

## Key Architecture Decisions

- **Pure client-side, no backend, no database** — one scorekeeper, one device, often offline. A server would add hosting, latency, and an offline failure mode while buying nothing for v1. Trade-off: diverges from Roger's usual Next.js + Prisma stack, but re-platforming a small app later is cheap. (Decision 2026-06-02.)
- **Local-storage persistence** — current game only; no cross-game data, consistent with stats being deferred.
- **Single-page app** — setup → play → end-game, all client-rendered.

## Technology Stack

- **React + Vite** — lightweight SPA, fast dev, simple PWA story. (To be confirmed at build time; chosen over Next.js because there is no server-side need.)
- **PWA** (service worker + manifest) for offline use and home-screen install.
- **localStorage** for persistence.
- Dev server must run with `--host` (Roger's WSL2 → Windows browser setup).

## Security Considerations

- No accounts, no personal data beyond freely-typed player names, no network calls — minimal attack surface.
- All data stays on the device in local storage; nothing transmitted.

## Performance Requirements

- Trivial load: a handful of players, dozens-to-hundreds of rounds at most. No performance concerns; target is instant interaction and sub-second cold start.

## Dependencies & Risks

- Minimal third-party dependencies (React, Vite, the PWA plugin — now wired). Keep the tree small. Two dev-only test dependencies were added in the Phase 3 hardening (jsdom and @testing-library/react) so the real app shell can be mounted in a browser-like test; neither ships in the production app. The PWA icon generation adds **no** dependency — it's a hand-rolled zero-dep Node script (`scripts/generate-icons.mjs`, run only when regenerating placeholder art). The two self-hosted display fonts (Nunito + Baloo 2, Open Font License) are bundled assets in `public/fonts/`, not npm packages.
- Main risk is **scoring-rule correctness**, not infrastructure — the engine must be thoroughly unit-tested against the rules and edge cases (Assaf ties, multiple catchers, 100-halving, elimination).
- **Known dev-toolchain advisory (flagged 2026-06-02, not shipped):** `npm audit` reports vulnerabilities in the Vite/Vitest/esbuild dev chain (one rated critical, in the Vitest UI server). These affect only the local development and test tooling, never the built PWA, and are unrelated to the dependencies added in Phase 3. Recommend addressing during a tooling-upgrade pass before any public deploy; not a blocker for current build phases.

## Current Status

- **v1.0 SHIPPED and LIVE (2026-06-03).** The app is released and running in public.
  - **Live app:** https://yaniv-scorekeeper.netlify.app
  - **Public repo (Roger's personal GitHub):** https://github.com/roger2o/yaniv-scorekeeper
  - **Release tag:** annotated `v1.0` at commit `1fb3b39`, fingerprint-verified to match the live deploy. The tag is the fixed record of exactly what shipped, because installed devices run fully offline and cannot be patched after install.
  - **What shipped:** the scoring engine (including mid-game player join); crash-safe local-device save (a refresh or app-close never loses a game); a themed two-view interface (a circular "players around the table" view for reading the score across the table, plus a vertical paper-style scoresheet) with two toggleable looks — **Felt & Chips** and **Party Arcade**; colour-coded round outcomes (Assaf in red, Yaniv in green); an in-app Help screen carrying both **How to Use** and **How to Play**; and an installable, fully-offline app (a PWA — install once over the internet, then it works with no connection at the table) with an online landing page that walks a visitor through installing on Android and on iPhone/iPad (Safari).
  - **Quality:** full test suite green at **376 tests** with a clean production build. Built and reviewed by the agent team (Turing built it; Holmes, Twiggy, and Bugsy reviewed; Wells deployed).
  - **Verified on a real device:** Roger confirmed the on-device install and the airplane-mode (fully offline) test both passed on his own phone.
  - **Hosting:** Netlify auto-deploy from `main` is enabled, so merges to `main` publish automatically.
  - **Deferred follow-ups (deliberate, not gaps):**
    - **Dev-tooling security bump** (Vite/Vitest) — **deferred by Roger's decision.** These advisories affect only the local build-and-test tooling and never the installed app a player runs; no user is exposed. To be picked up in a future tooling pass.
    - **Final app icon** — the shipped home-screen icon is a deliberate **placeholder**. Final artwork can be dropped in later with no code change (`public/icon.svg` is the editable source).
    - **Formal team device-testing** — the real-device-only UX items (across-table legibility, the louder Party Arcade look, rotated scores at 5–6 players) are **eyeball-confirmed by Roger on his own phone** (and the offline behaviour is confirmed), but have **not** yet been put through a formal device-test pass by the team.

- **Landing-page UX/accessibility fixes from Twiggy's review (2026-06-03, Turing).** Applied Twiggy's findings to the public landing page; the protected PWA files (`vite.config.ts`, the manifest, the SW registration) were **not** touched. **The headline change — the iPhone "must use Safari" instruction now looks as urgent as it is.** It was previously washed in the same soft accent tint as the friendly worked-example box, so the single most install-critical line on the page read like a gentle aside. It is now a distinct, visually heavier **warning box** — a saturated amber surface, a thick contrasting border, an elevation shadow, and a ⚠ glyph — that out-weights every other box on the page. Meaning is never carried by colour alone: the glyph sits beside the word "Important" and bold text. This uses a new `--warn` / `--warn-ink` / `--warn-border` token trio defined per theme, both **clearing WCAG AA** (warning text on the amber: **8.42:1** on Felt & Chips, **9.87:1** on Party Arcade; the delineating border clears the 3:1 graphic bar in both). **The Safari requirement is also surfaced earlier:** the iPhone section heading now reads "Install on iPhone / iPad — Safari only", and a visitor we can confidently detect as being on an iPhone/iPad in a *non-Safari* browser (best-effort, conservative — it stays silent on any uncertainty so a desktop/Android/Safari visitor never sees a wrong warning) gets a short warning banner floated near the very top, so they learn why nothing installs before scrolling the whole page. **Smaller accessibility wins:** the two identical "Start scoring now" buttons now read distinctly to a screen reader (the footer one is "Start scoring now — skip the guides"); a standard **"skip to content"** link was added at the top of the long page; the landing now exposes correct page-level **banner / main / contentinfo** landmarks (its header and footer were previously demoted by sitting inside the app's generic `<main>`); and the card-values table in the rules guide gained an accessible **caption**. The "Why install" list already led with the offline benefit, so no copy reorder was needed. Per Twiggy's explicit guidance, per-word language tags on "Yaniv"/"Assaf" were deliberately **not** added (they harm screen-reader pronunciation of transliterated terms) and the sessionStorage dismissal + standalone gate were left exactly as they were. **Full suite green at 376 tests** (9 new landing tests: the iOS warning is the prominent warn box not the soft callout, the Safari requirement is in the heading, the early banner shows for Chrome-on-iOS and stays hidden for genuine Safari and for desktop, the two CTAs have distinct accessible names, the skip link is present and targets main, the banner/main/contentinfo landmarks are correct, and the table has a caption), clean `tsc -b` and production `vite build` (PWA generation unchanged, app-shell precache intact). Files: `src/landing/LandingPage.tsx`/`.css`, `src/landing/installState.ts` (+ `index.ts` export), `src/App.tsx`, `src/content/helpContent.tsx`, `src/content/helpProse.css`, `src/theme/tokens.css`. Branch `yaniv-themed-ui`; local git only, no remote. *Handoffs: Holmes (code review — the conservative iOS-non-Safari UA detection and the landmark restructure) and Bugsy (the new detection + the early-banner gating are the main new logic to harden). Note for Twiggy's device queue: the warning box's prominence and the Theme B amber/red look still want a real-phone glance.*
- **Review fixes — honest font weights + PWA cache hardening (2026-06-03, Turing).** A pre-cleared correctness pass from Holmes's review. **The font fix (the one that mattered):** Theme B's headings ask for the heaviest "Party Arcade" bold (weight 800), but the bundled Baloo 2 font file was *declared* as only going up to 700 — so browsers were faking the bold by stretching the 700 weight, which renders thin and inconsistent. I inspected the actual font file and confirmed it genuinely contains the full weight range up to 800 (the slimming-down step that shrinks the file for offline use did **not** drop the heavy master). So the fix was to correct the declaration to tell the truth about the file (extend it to 400–800), giving the real, intended heavy bold with no faking — **no font file change, no size change, still precached**. I did the same sanity check on the other font (Nunito): its file spans an even wider range than declared and every weight the UI uses is already covered honestly, so only a clarifying comment was added. **Three small PWA hardening tweaks:** the offline page-shell cache now refuses to ever store a failed (error) response, so a momentary server hiccup can never poison the one guarantee that matters — the app always opens offline; a stray, never-shipped file type was dropped from the precache list for accuracy; and the iPhone home-screen icon link now states its size explicitly (verified the icon really is that size). **Full suite green at 367 tests** (no test changes needed — these are declaration/config-level fixes), clean `tsc -b` and production `vite build`, and the generated service worker + manifest still build correctly with both fonts precached and the build size unchanged (~329 KiB). Branch `yaniv-themed-ui`; local git only, no remote. *Note for Twiggy: the Theme B heading weight should still be eyeballed on a real device now that it renders the true 800 instead of a faux-bold.*
- **Online landing page built — the pre-install browser entry point (2026-06-03, Turing).** Opening the app's shared URL in a normal browser tab now shows a themed landing page *before* anything is installed. It carries, top to bottom: a **hero** (name + the one-line "keep the score, not the paper" summary), **"Why install?"** (works offline, full-screen, no app store/account), step-by-step **Install on Android** (use Chrome → Install / Add to Home screen) and **Install on iPhone/iPad** (leading with the gotcha — you **must use Safari**, since Chrome/others can't install a PWA on iOS — then Share → Add to Home Screen), and **both** guides — **How to Use** and **How to Play** — rendered from the **same single-source content blocks** the in-app Help uses (`src/content/helpContent.tsx`), so the landing and the in-app Help can never drift apart. A **"Start scoring now"** button (offered at the top and again after the guides) lets a visitor use the app immediately in the browser without installing. **Install/standalone behaviour:** when the app is launched installed/standalone (`display-mode: standalone`, or iOS `navigator.standalone`) the landing is skipped entirely and the app opens straight away; a browser visitor sees the landing, and "Start scoring now" dismisses it for the rest of that visit (sessionStorage) while a genuinely fresh visit can still see it. Themed on the existing token layer (Felt & Chips / Party Arcade — the theme toggle is offered on the landing too); mobile-first at 375px; the WCAG 2.1 AA / interaction floor is inherited (≥56px primary action, focus-visible, reduced-motion, semantic landmarks + headings, AA contrast, no pure #000/#fff). Terminology is correct — never "deal/dealer" (the single "dealt 5 cards" is the rules-of-the-physical-game copy inside How to Play, the explicitly allowed exception). To avoid duplicating styles, the shared guide-prose CSS was lifted out of `HelpDialog.css` into `src/content/helpProse.css` and imported by **both** surfaces so the guides render identically. Files: `src/landing/` (`LandingPage.tsx`/`.css`, `installState.ts`, `dismissed.ts`, `index.ts`); gate wired in `src/App.tsx`. **The protected PWA files were NOT touched** (`vite.config.ts`, `src/pwa/*`, `src/main.tsx` SW registration, the manifest, index.html's PWA bits — under separate review). **Full suite green at 367 tests** (10 new landing tests: browser-renders vs. standalone-bypass, both guides render and are byte-for-byte the shared source, Android + iOS install sections incl. the Safari gotcha, "Start scoring now" enters the app, dismissal persists within the visit, and no "dealer"), clean `tsc -b` + production `vite build` — and because the landing is part of the precached app shell it works offline too. Branch `yaniv-themed-ui`; local git only, no remote. *Handoffs: Twiggy (UX of the landing — install-step clarity, the Safari gotcha's prominence, the on-device look in both themes) and Holmes (code review — the standalone/dismissal gate logic and the prose-CSS extraction).*
- **The app is now an installable, fully-offline PWA (2026-06-03, Turing).** Phase 10's build items are done: `vite-plugin-pwa` is wired into the Vite 5 config (Vite was deliberately NOT bumped — that upgrade is Wells's separate deploy-prep task) with an **autoUpdate** service-worker strategy (skipWaiting + clientsClaim; registered via `registerSW({ immediate: true })` in `main.tsx`). The practical effect for Roger: each device installs its own copy from the URL, runs fully offline at the table, and — critically — **silently picks up the newest version on its next refresh**, so a future scoring-bug fix can actually reach devices that can't otherwise be patched (the classic "stale app forever" trap is closed by design). The production build now generates a service worker (`dist/sw.js`) and a web app manifest (`dist/manifest.webmanifest`) and **precaches the entire app shell — 22 files, ~321 KiB** (the page, all code/styles, icons, manifest, and fonts), so the first online load makes the app work offline forever after; the page itself is fetched **network-first** (with a fast fallback to the cached copy) so a new build is found on the next online load. **Manifest:** name "Yaniv Scorekeeper" / short name "Yaniv", standalone full-screen, portrait, with the default **Felt & Chips** felt-green (`#1f4a3d`) as both the theme and splash colour (real theme tokens, no pure black/white). **Icons:** a real set — 192px, 512px, a 512px **maskable** icon (needed so the home-screen icon isn't clipped on Android/iOS), plus an apple-touch-icon and a favicon. **The icon art is a sensible PLACEHOLDER** — a card-table motif (felt tile, brass rim, two ivory cards with a red diamond and a dark club) in the brand palette, generated by a tiny zero-dependency script (`scripts/generate-icons.mjs`) with an editable `public/icon.svg` source — **Roger can replace it with final art later** by dropping in new files of the same names. **Fonts are now self-hosted locally** (Nunito + Baloo 2, the two display faces the UI names; ~72 KB, Open Font License) and precached, so they render the same offline as online — no Google Fonts CDN link, which would silently fail with no connection. autoUpdate needs no "new version available" prompt, so none was built (deliberately not over-built). **Full suite green at 357 tests** (3 new manifest-shape tests guarding name/colours/the maskable icon), clean `tsc -b` and production `vite build`. Branch `yaniv-themed-ui`; local git only, no remote. **Important caveat:** a service worker and home-screen install only work over HTTPS or localhost, so the *actual* install + airplane-mode offline test must run against the Netlify HTTPS preview (Wells), **not** the WSL2 LAN dev server — that verification is still outstanding. *Handoffs: Wells (PWA/deploy-readiness review + the eventual install/offline verification on the Netlify preview) and Holmes (quick config code review).*
- **Two small review tweaks to the themed UI — compact scoresheet markers + an aria tidy (2026-06-03, Turing).** A follow-up to Twiggy's and Holmes's review of the Big Board scoresheet and the Play screen; the scoring engine and state core were not touched. (1) **Compact in-cell markers (Twiggy, major):** a single scoresheet cell can legitimately carry several markers in one round (the worst realistic case is an Assaf caller who also lands on an exact 100, showing both "Assaf" and "100→50"; join + "out" is another). Previously each marker was forced onto its own line, so a multi-marker cell grew several lines tall and blew out the whole row height in the narrow 56px phone column, hurting scannability. The markers now **flow inline and wrap** inside one tightened container — they sit side by side and only wrap when the column genuinely runs out of room — with a slightly smaller font and tighter line spacing. **No information was dropped — every marker is still present;** only the footprint is capped. The colour-coded "Assaf"/"Yaniv" words are unchanged in meaning and still clear WCAG AA (the size nudge from ~10.6px to ~10.2px keeps them in the same small-text contrast bracket, so the existing measured ratios still hold). (2) **Aria tidy (Twiggy, minor):** the "starts next" marker in the circle view carried a redundant `aria-hidden="false"` (false is the default and reads as if something is being toggled) — removed, no behaviour change. (3) **Noise cleanup (Holmes, optional):** the scoresheet body cells set five `data-*` flags; only `data-yaniv` is actually read (by the Big Board tests, to confirm the Yaniv-winner marker lands on the right cell) — it was kept. The other four (`data-assaf`/`data-halved`/`data-eliminated`/`data-joined` on the body cell) were referenced by nothing and were removed. **Full suite green at 354 tests** (one new Big Board test verifies the worst-case Assaf + 100-halving stack renders both markers inside the single compact wrapping container), clean `tsc -b` and production `vite build`. Branch `yaniv-themed-ui`; local git only, no remote. *These were small, pre-cleared review items; no further review needed unless the on-device look of the wrapped markers at 5–6 players surprises Twiggy.*
- **Scoresheet outcome markers are now colour-coded WORDS — "Assaf" (red) / "Yaniv" (green) (2026-06-03, Turing).** Per Roger, the two round-outcome markers in the Big Board cells were changed from glyph-led short codes to small colour-coded words: the Assaf marker (previously "+30") now reads the **word "Assaf" in small red letters**, and the successful-Yaniv marker (previously "★ Yaniv", gold) now reads the **word "Yaniv" in small green letters**, both in the relevant player's cell for that round. The word carries the meaning and the colour reinforces it — never colour-alone, so it stays colour-blind-safe and accessible. The full screen-reader meaning is preserved on the aria-labels ("Assaf — caught, plus 30 penalty" and "Successful Yaniv — won the round") so the +30 sense isn't lost to assistive tech even though the cell shows just the word. The other markers (mid-game join, elimination "out", leader crown, 100-halving "100→50") are unchanged. **New per-theme red/green text tokens** (`--outcome-assaf-text` / `--outcome-yaniv-text`) were added so the words clear **WCAG AA (≥4.5:1 for small text)** against the raised cell background in BOTH themes; a saturated red/green can't clear AA on the medium-dark felt, so Felt uses a lighter salmon-red and a brighter mint-green (legibility wins over exact hue, as the brief allowed). **Measured ratios — Assaf red: Felt 4.73:1, Arcade 5.42:1; Yaniv green: Felt 5.26:1, Arcade 5.35:1** (all clear AA). The words use the existing small marker font size and are allowed to wrap in the narrow phone column. Engine/state untouched (markers derived from each resolved round's `outcome` + `callerId`). **Full suite green at 353 tests** (the Big Board Assaf-marker and Yaniv-vs-Assaf distinction tests updated to assert the cells show the words "Assaf"/"Yaniv" and keep the descriptive screen-reader labels), clean `tsc -b` and production `vite build`. Branch `yaniv-themed-ui`; local git only, no remote. *Awaiting review: Holmes (markup/CSS correctness, token contrast) + Twiggy (on-device legibility of the red/green words in both themes) tomorrow, alongside the queued device items.*
- **Circle view — players now spread EVENLY around the ring (2026-06-03, Turing).** Roger spotted that in the circle (Play/viewing) view the players around the ring were not evenly spaced — they bunched toward the bottom and the top corners and left gaps. **Cause:** the seat layout snapped each player's *position* to one of a handful of fixed edge/corner anchors (those anchors had been chosen to keep text at a legible 0/90/180/270), so the seats never sat at their true even angles — e.g. 3 players landed bottom/left/right on the horizontal midline instead of a clean triangle, and 5–6 crowded the top corners. **Fix:** position and text rotation are now **decoupled**. Each seat is placed at its exact even angle — 360°/N apart — on a fixed ring radius, while only the *text rotation* is snapped to the nearest cardinal for legibility (even position, legible rotation). The scorekeeper stays anchored upright at the bottom, the rest fill round in the same seating order as before, the 2-player top/bottom face-off is preserved, and 3/4/5/6 players are all evenly spaced. Chips stay inside the existing ring sizing (the new radius matches the old left/right anchor distance, and the 5–6 seats are pulled *inward* versus the old corners, so nothing overflows). Static seating order, the leader crown, and the "starts next" marker are untouched, and the engine/state core was not touched (layout-only). **Full suite green at 353 tests** (27 new `ringLayout` tests assert even ~360°/N spacing and equal radius for 3–6 players, the scorekeeper at the bottom upright, legible-cardinal rotations, chips inside the container, and the 2-player face-off + 7+ big-board fallback), clean `tsc -b` and production `vite build`. Local git only; no remote. *Awaiting review: Holmes (layout math) + Twiggy (on-device look of the even ring at 5–6 players) tomorrow, alongside the queued device items.*
- **Successful-Yaniv WINNER marker added to the Big Board scoresheet (2026-06-03, Turing).** The scoresheet already flagged an Assaf in the caller's cell but had NO marker for a *successful* Yaniv. Added a matching in-cell pill — a star glyph + the word "Yaniv" — on the cell of the round's caller whenever the round resolved as a Yaniv (they scored 0 and won). It mirrors the Assaf marker's treatment (small in-cell pill, glyph + text, never colour alone) and is styled in the existing token system so it works in BOTH themes. **Colour:** the app's celebration gold (`--celebrate`) with the dark warm score-chip ink (`--chip-text`) — semantically the "win" colour, and visually DISTINCT from the red Assaf/out markers, the brass/grape accent markers (leader crown, halving, starts-next), and the soft-wash join marker. The text/background pair clears WCAG AA in both themes (Felt 9.5:1, Arcade 7.47:1). In-cell copy is kept short ("★ Yaniv") for the narrow phone column; the full meaning ("Successful Yaniv — won the round") rides the aria-label. Engine and state untouched — the marker is derived from each resolved round's `outcome` and `callerId`. **Full suite green at 326 tests** (one new Big Board test: a Yaniv round renders the winner marker on the caller's cell, and an Assaf round renders the Assaf marker and NOT the Yaniv one), clean `tsc -b` and production `vite build`. Local git only; no remote. *Awaiting review: Holmes/Twiggy pass on this marker can run tomorrow alongside the queued device items.*
- **Holmes + Twiggy review fixes applied to the themed UI (2026-06-03, Turing).** A correctness + accessibility pass on the Big Board scoresheet and the in-app Help dialog; the scoring engine and state core were not touched (read-only from derived state). **The one correctness bug:** a player who joined *after* the last recorded round (a "pending" join, still shown in standings) was misdrawn on the scoresheet as an original player with a full column of zeros — the board only read joins recorded on rounds and ignored the pending-join list. Fixed so that latecomer's history cells render BLANK like any not-yet-active player and their seed shows in the current-totals row, exactly like a join that lands on a recorded round. **Big Board UX:** the live standings now have a polite screen-reader announcement when a round lands (previously only the special Assaf/halving/elimination/join moments were announced, so a routine round was silent for a non-sighted user); the current leader is now reinforced on the "Now" current-totals row with a crown + the word "leader" (not just the soft header tint), since that row is where the eye goes for who's winning; the in-cell special-moment pills were shortened ("Assaf +30" → "+30", "joined · seed 80" → "join 80") so they don't clip in a narrow phone column, with the full wording kept in the round note and on screen-reader labels; a "scroll sideways for more players" hint appears only when the columns actually overflow the screen (6+ players on a small phone) so nobody assumes the visible columns are the whole table; the empty state reads "Nothing scored yet — play a round!"; and long player names wrap to two lines instead of truncating, so two similar names are distinguishable on a phone. **Help dialog:** the close button and the two tab buttons were raised to the project's 56px touch-target floor; the share fallback (when both the native share sheet and clipboard copy fail) now renders the link as a real selectable/long-pressable link instead of plain text; the intro copy was clarified ("your game keeps going in the background" → "your game is safe, nothing is lost"); the hardcoded modal backdrop colour was replaced with a new `--scrim` theme token defined for both themes; and a code comment was added flagging a latent focus-trap footgun (the `offsetParent` visibility check would wrongly exclude any future fixed-position control — correct today). All new text/background colour pairs were checked against WCAG AA (leader pill 6.8:1 / 4.9:1; overflow hint 6.8:1 / 5.7:1; share link 8.5:1 / ~14:1 — the brass-on-felt accent was swapped to ivory because it measured 4.12:1, just under the floor). **Full suite green at 325 tests** (a new Big Board test covers the after-the-last-round join case; existing join/Assaf/empty-state tests updated for the new copy), clean `tsc -b` and production `vite build`. Local git only; no remote. *Out of scope this pass (device-only, need a real phone): Twiggy's two-axis sticky rendering check, Theme B seat colours at 3:1, and rotated-chip legibility at 5–6 players. Handoffs: Twiggy (the device-only items remain) and Holmes (re-confirm the pending-join render fix).*
- **Big Board redesigned as a VERTICAL SCORESHEET (2026-06-02, Turing).** The Big Board view changed from a one-total-per-player list into a full scoresheet grid that mirrors the handwritten paper scoresheet the app replaces. **Players are columns** across the top in **static seating order** (never reordered by score); the current **leader is indicated** on their column header (crown + the word "leader") and is **never repositioned**; **eliminated** players are marked **"out"** with real text, not colour alone. **Rounds are rows** going down the page, oldest at top → latest at bottom, with vertical scroll for long games. **Each cell is that player's running cumulative total after that round** (tabular-nums) — the number you'd keep updating on paper. A **left "round" column** carries the round number and a compact note of what happened (who called "Yaniv!" and whether it resolved as a Yaniv or an Assaf — never "deal/dealer"). **Small accessible in-cell markers** flag the special moments — Assaf (+30), a 100-halving (e.g. "100→50"), an elimination, and a mid-game join (with the derived seed) — each a glyph **plus** text, never colour alone. An **emphasised current-totals footer row** shows the latest standings with a clear **"starts the next round"** indicator. A **mid-game joiner's** column is blank (—) for rounds before they joined and shows their seed at the join point; the grid handles the player set growing. **This replaces the deferred round-by-round history log — the scoresheet is the history now.** It is a semantic `<table>` with a `<caption>`, `<th scope="col">` per player and `<th scope="row">` per round, and works in both themes via the existing token layer. Mobile-first at **375px**: narrow columns with tabular-nums, long header names truncated (full name via `title`/aria), primarily **vertical** scroll with a **sticky header row and sticky round column**, falling back to horizontal scroll only when up-to-6-player columns genuinely overflow. Reads entirely from existing **engine-derived state** (`game.rounds`, `game.standings`) — the engine and state core were not touched. **12 new scoresheet tests** (row-per-round, correct running totals, Assaf/halving/elimination/join markers, static seating order, leader indicated-not-repositioned, joiner blank-then-seeded, no "dealer"); **full suite green at 324 tests**, clean `tsc -b` and production `vite build`. Local git only; no remote. *Handoffs: Twiggy (UX/device review — the 6-player width at 375px is the key risk) and Holmes (code review).*
- **In-app Help screen built (2026-06-02, Turing).** A **"?" / Help** control now lives in the top bar of **both** the Play and Setup screens (a round, clearly-tappable button at the 56px touch-target floor, labelled "Help" for screen readers). It opens a **Help dialog** with two clearly-labelled tabs — **How to Use** (operating the scorekeeper) and **How to Play** (the rules of Yaniv) — plus a **"Share this app"** action that uses the **Web Share API** where available (so a player can hand the app's link to the next table to install it) and falls back to copying the link, then to showing the link, where the API isn't available. The two guides are rendered from **single-source content blocks** (`src/content/helpContent.tsx`, Quill's finalized Section A / Section B copy verbatim) so the same source will feed the in-app Help and the later landing page and they can never drift apart. The dialog meets the accessibility floor in full: `role="dialog"`, `aria-modal`, `aria-labelledby`, a focus trap, Escape-to-close, backdrop-click-to-close, **focus returns to the "?" trigger on close**, and the tabs follow the WAI-ARIA tabs pattern (roles, `aria-selected`, arrow-key navigation). Terminology is correct throughout — a player "starts the next round", never "deals/dealer". Both themes skin it via the existing tokenized layer. **Full suite green at 312 tests** (12 new Help interaction tests covering: opens from both screens, both tabs render their key content, Escape closes + focus returns, and Share invokes `navigator.share` when present and degrades gracefully when not), clean `tsc -b` and production `vite build`. Local git only; no remote. *Handoffs: Twiggy (UX review of the Help screen) and Holmes (review incl. the dialog focus management).*
- **Review fixes applied (2026-06-02, Turing) — accessibility + correctness pass on the themed UI.** Four reviewer findings were fixed without touching the scoring engine or state core: (1) **Theme A "Assaf" red failed the contrast floor** — ivory text on the old red measured 4.08:1, below the 4.5:1 AA minimum, so the Assaf callout, the danger button, and the danger banner all fell short; the Theme A danger red was darkened (`#c8453b` → `#b3382f`) so the same ivory text now clears AA at **5.06:1**. Theme B's red was already compliant (4.69:1) and was left alone. (2) **The leader "crown" was not reliably announced to screen readers** — it relied on a label attached to a decorative symbol, which assistive tech reads inconsistently; both the circle view and the big-board now show the crown as decoration paired with a real "leader" text equivalent. The eliminated "OUT" marker was already real on-screen text. (3) A leftover developer scratch test file was removed (already gone from disk); the legitimate hardening test suite is retained and committed. (4) **The end-game winner could disagree with the live leader** shown during play — it sorted across all players including knocked-out ones; the end screen now picks the winner with the exact same rule as the live scoreboard (lowest score among still-active players, ties broken by seat order), and a guard was added so the stats line is safe when there are no players. Full suite still green: **300 tests**, clean `tsc -b` and production `vite build`. Local git only; no remote.
- **UI build complete (2026-06-02) — Phases 5–9, Turing.** The full themed two-view UI is built on top of the verified engine and state store, replacing the placeholder stubs. **Screens that now exist:** a **Setup** screen (fast player add with auto-focus + Enter/＋, "Player N" defaults, threshold segmented control, halving toggle on by default, Advanced knockout disclosure; player ids generated independent of name so duplicates can't collide); a **Round-entry** flow (custom fixed on-screen number pad — never the native keyboard — with the three locked steps: tap who called → enter each hand with the caller's required and blank-vs-0 distinct → a confirm/review step showing the resolved Yaniv/Assaf outcome, points, and who starts next *before* committing; soft non-blocking flags for an above-threshold caller and implausible totals); a **Play** screen with the signature **circle view** (players around a ring in static seating order, each score rotated to face its seat snapped to 0/90/180/270, scorekeeper upright at the bottom, who-starts-next marked by ring-glow + arrow + the words, leader crowned, a centre ＋ New Round button) plus an upright **big-board** semantic-`<table>` fallback for 7+ players (and a manual toggle), non-blocking `aria-live` callouts (Assaf / 100-halving / elimination / mid-game join), an add-player-mid-game affordance, and undo; and an **End-game** screen (winner = lowest cumulative or sole survivor, final standings lowest-first in a semantic table, per-player successful-Yaniv count, reduced-motion-gated confetti in Theme B, New game / Rematch). **Two themes** ("Felt & Chips" and "Party Arcade") sit on a shared tokenized CSS-variable layer (`src/theme/`); the toggle is a persisted per-device preference under its own localStorage key, separate from game data; both clear WCAG AA and the interaction/motion floor (≥56px pad keys, `:active` press feedback, hover gated to hover devices, `prefers-reduced-motion`, focus-visible rings). **Store contract extended** (engine untouched): `addPlayer`/`removePlayer` actions for mid-game join + recovery, and a derived `engineError` so an edit/undo that strands a joiner shows a plain message and a one-tap recovery instead of crashing. **Full test suite passing** (~300 tests across engine, state, persistence, and component/interaction coverage), clean `tsc -b` + production `vite build`. **Deferred from this build:** the round-by-round history log (a read-only detail panel). Local git only; no remote (Wells owns remote/deploy). Run locally with `npm run dev -- --host`.
- **Latest (2026-06-02):** The scoring **engine, application state layer, and crash-safe persistence are complete and verified** — full suite of **203 tests** passing with a clean TypeScript + Vite build. Two threads are now live: (1) the **mid-game-join engine change is in progress** (Turing is extending the model so the player set can grow mid-game and deriving the joiner's seed at recompute time); (2) the **UI design direction is approved** (two themes on a shared tokenized layer, two-view circle/table model — see UX Considerations) and the **themed, two-view UI build is queued next**.
- **In build — Phases 0, 1, 2 & 3 complete (2026-06-02).** React + Vite + TypeScript scaffold in place (Vitest test runner, `vite-plugin-pwa` installed but not yet wired, dev server binds `--host`). The pure scoring engine is built under `src/engine/` as a single `recompute(history, settings)` function that derives all game state from the round-history list. Bugsy's adversarial suite and Holmes's engine review are done; Holmes's engine review fixes are applied (see below). **Phase 3 adds the application state layer and crash-safe persistence under `src/state/`, plus a thin app shell (`src/screens/`) that switches setup → play → end.** Full suite now passes **203 unit tests** with a clean TypeScript + Vite build. Local git repo only; no remote yet (Wells owns remote/deploy).
- **Phase 3 — state & persistence (2026-06-02):** A minimal Context + reducer store holds only the source-of-truth (settings + round-history + a screen marker) and derives all displayed state (standings, who-starts-next, halving callouts, eliminations, successful-Yaniv counts, winner) by calling `recompute` — derived state is never stored. Actions: start game, add round, undo last round (most-recent-only), edit last round (most-recent-only), end game, reset. Persistence is **versioned** (a schema `version` is stored alongside the state) and **fully try/catch-wrapped**: if localStorage is unavailable or a write fails (iOS Private Mode, embedded webviews, quota), the game keeps running in memory and a non-fatal warning is surfaced — it never crashes. On load, a valid in-progress game is restored; missing, corrupt, or incompatible-version saves are discarded gracefully and the app starts clean at setup. **The screens are deliberate PLACEHOLDER stubs** (plain text + bare buttons) that exist only to drive the flow — the real UI (setup form, number pad, standings table, winner screen) is Phases 4–7 and will be designed and reviewed before it is built.
- **Restore-path crash-safety fix applied (2026-06-02):** Holmes and Bugsy both found a way a corrupted or hand-edited saved game could white-screen the app on load. The saved-game check only confirmed the *shape* of the data, not that it formed a legal game, so a structurally-valid-but-illegal save (e.g. a single player, broken seating, or a round recorded after the game had already ended) would load and then crash while drawing the screen. The restore path now runs the saved game through the scoring engine once before accepting it; anything the engine rejects is discarded, storage is cleared, and the app falls back cleanly to the setup screen carrying a non-fatal "previous game couldn't be restored" notice (so the eventual UI can explain the loss rather than failing silently). The screen-drawing step was also wrapped so it can never crash the app even mid-game. An unused internal restore action was removed so there is a single, well-guarded restore path. New tests mount the real app shell in a browser-like environment and confirm every known bad-save case falls back without crashing and clears the poisoned data; the white-screen-on-bad-save crash is gone. (Engine untouched.)
- **Holmes review fixes applied (2026-06-02):** the multiple-catcher tie-break now ranks players by their position around the seat circle (matching the who-starts-next logic exactly), so it can never pick the wrong starter; player setup now enforces that seats are a clean 0,1,2,… sequence with no gaps, negatives, or fractions; and a round that lists a hand for someone who isn't an active player (a typo or a knocked-out player re-entered) is now rejected instead of silently ignored.
- **Next:** the **mid-game-join engine phase** (in progress, Turing — extend the model, derive the seed, no-halving-on-seed, replayable join event with idempotent recompute, full tests, Holmes review), then the **themed two-view UI build** (Setup / Round entry / Standings / End game) against the approved design direction.

## Resolved Decisions (2026-06-02)

- **Multiple-catcher tie-breaking** — confirmed: lowest hand among the catchers starts the next round; ties broken by seating order after the caller.
- **Round editing** — confirmed: most-recent-round-only for v1 (no editing of arbitrary past rounds).
- **Distribution model** — confirmed (Reading A): the app is given to many people who each run their own **independent** game on their own device, concurrently and offline. No shared/synced game across devices (that remains deferred). Delivered as an installable PWA from a static URL; after first install each device is fully self-contained.
- **Halving — no cascade** — halving fires at most once per player per round. A total landing on 200 halves to 100 and stops (it does **not** continue to 50), even though 100 is itself a multiple of 100.
- **Halving vs. elimination order** — halving is applied **before** the elimination check. If an (optional) elimination score is itself a round multiple of 100 and a player lands on it exactly, the halving triggers first and rescues the player from being knocked out that round.
- **Elimination boundary** — a player is eliminated only when their cumulative total is **strictly greater than** the elimination score, never when equal to it.
- **Undo/edit = replay from history** — the round-history list is the single source of truth; cumulative totals, halving callouts, eliminations, who-starts-next, and the per-player successful-Yaniv count are all derived by recomputing from history, never by patching deltas in place.
- **Standings in static seating order (no score sort)** — standings are shown in the fixed order players sit around the table and are **never reordered by score**. The leader is only indicated (crown/highlight), not repositioned. *Why:* seating is the stable physical anchor; reordering would make the on-table scoreboard confusing to read at a glance. (Corrects the earlier "sorted lowest-first" spec; the engine already returns standings in seat order, so this is a display/spec correction only.)
- **Mid-game join** — a player may join an in-progress game, seeded at the highest cumulative among still-active players; no halving on the seed; normal play (incl. future 100-halving) thereafter; never instantly eliminated; takes the next seat (the ring grows); recorded as a replayable history event with the seed derived (never stored). *Why:* lets a latecomer join a live table fairly without a head start, while preserving the recompute-from-history guarantee. (Full rules in Scoring Engine.)
- **Two themes on one tokenized layer (A + B)** — two user-toggleable skins, "Felt & Chips" (A) and "Party Arcade" (B), on a single shared theme layer; toggle is a persisted per-device preference; both held to WCAG AA contrast and across-table legibility. *Why:* gives the app personality and player choice without forking the layout or compromising legibility.
- **Two-view model (circle = view / table = entry)** — a circle view for viewing during play (phone flat, scores oriented to each seat) and a table/list view for entering scores (number pad + confirm-round step); both in static seating order. *Why:* the resting scoreboard must read from all sides of the table, while fast entry needs a single upright orientation — different jobs, different views.
- **Circle ring is seat-fixed; rank is indicated, not reordered (Twiggy's recommendation, adopted)** — the ring stays in fixed seating order so each player's chip stays in their physical direction (rotation is only meaningful that way); the leader is shown with a crown and the big-board list is the escape hatch for tight tables. The circle never physically reorders by score. *Why:* honours "static seating order" and keeps the signature per-seat orientation; the upright big-board `<table>` simultaneously satisfies the WCAG semantic-standings requirement.
- **Landing page + in-app Help, sharing single-source copy** — the app ships with a public landing page (on the same static URL it's distributed from) carrying the "why install", Android and iPhone/iPad install steps, and both how-to bodies; and an in-app Help screen carrying the same two bodies. The copy is written **once** per body and reused in both places. *Why:* the audience is non-technical casual players who need install hand-holding (and iPhone users specifically must be told to use Safari, since other browsers can't install a PWA on iOS), and the in-app copy means a player can re-check things mid-game with no signal. Single-source copy prevents the landing page and the app from disagreeing. Drafted copy lives in `docs/landing-and-help-content.md`. Consistent with the URL/PWA distribution model — the landing page *is* the install entry point each device uses. (Decision 2026-06-02.)
- **"How to Use" and "How to Play" are two separate bodies of copy** — the help content is split into two independently-titled guides: **How to Use** (operating the scorekeeper — set up, enter a round, read standings + who-starts-next, undo, add a player mid-game, end game + the per-game successful-Yaniv stat, switch themes) and **How to Play** (the rules of Yaniv itself, drawn from `YanivGameRules.txt` — object, card values, a turn, calling Yaniv, Successful Yaniv vs. Assaf with the +30 and a worked example, winning, and the common house rules). Both surfaces (landing page and in-app Help) show **both** bodies, clearly distinguished; the in-app Help uses two labelled tabs/sections. *Why:* Roger clarified the original draft was really "how to use the app" and that the game's rules are a separate thing — a seasoned player only needs How to Use, while a newcomer needs How to Play. Mixing them muddied both. Keeping them apart (and never mixing rules-teaching into the app-operation copy, or app buttons into the rules copy) keeps each scannable for its reader. (Decision 2026-06-02.)
- **Store contract extended for mid-game join (Turing, UI build)** — the state store gained `addPlayer(name)` (mid-game join: builds a Player with a name-independent id, the next seat, and the join marker; the engine derives the seed) and `removePlayer(id)` (recovers from an edit/undo that strands a joiner), plus a derived `engineError` exposed alongside `game`. *Why:* the engine already supports joins via `Player.joinsBeforeRoundIndex`, but the store had no way to grow the player set after `startGame`; this is the minimal extension to wire the brief's mid-game-join and error-handling requirements. The pure engine and persistence layers were NOT changed.

### Build-time recommendations adopted (from team plan review, 2026-06-02)

- **Round entry uses a custom fixed on-screen number pad**, not the native keyboard (speed of the repeated entry loop is the core value).
- **A "confirm round" review step** is shown before each round commits (errors are common; undo is most-recent-only).
- **Hosting on Netlify**; PWA built with `vite-plugin-pwa` using an auto-update strategy to avoid the stale-service-worker trap; full app shell precached (self-hosted fonts) for reliable offline.
- **Accessibility target: WCAG 2.1 AA.**
- **Scoring engine + full unit-test suite built first**, before any UI — v1 correctness is the only defence for offline devices that can't be patched.

## Open Questions

- ~~**PWA install polish**~~ — RESOLVED (2026-06-03): service worker + manifest + full icon set are wired via `vite-plugin-pwa` (autoUpdate). Remaining: the on-device install/offline verification on the HTTPS preview (Wells), and replacing the **placeholder icon art** with final art when Roger has it (drop new PNGs + `icon.svg` of the same names into `public/`).
- ~~**Round-by-round history log**~~ — RESOLVED (2026-06-02): delivered as the vertical scoresheet (the redesigned Big Board), which is the round-by-round history. The separate read-only panel is no longer needed.
- **Default theme on first run** — currently "Felt & Chips" (A). Confirm with Roger whether the louder "Party Arcade" (B) should be the first-run default instead.
- ~~**Web-font loading**~~ — RESOLVED (2026-06-03): the two display faces (Nunito / Baloo 2) are now **self-hosted locally** (`public/fonts/*.woff2`, OFL, latin subset, ~72 KB total) with `@font-face` in `src/theme/fonts.css` and precached by the service worker, so they work offline. No external font CDN (a Google Fonts link would silently fail with no connection). `font-display: swap` keeps first paint unblocked.
- **Visual/device pass** — the UI is verified by the jsdom component tests + build, not yet by a real across-table device test (no headless browser in this environment). That across-table legibility pass is Phase 11 (Twiggy).
